const TopBar = () => {
  return (
    <div className="w-full h-12 flex items-center justify-between px-4">
      <h1 className="text-lg font-semibold">WordDash</h1>
      <button>Connect Wallet</button>
    </div>
  );
};

export default TopBar;
